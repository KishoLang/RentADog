<div class="d-flex justify-content-center">
  <h1>Rent a Dog </h1>
</div>
  <!--
  <%= link_to "Sign up", new_user_registration_path %>
  <%= link_to "Login", new_user_session_path %>
  <%= link_to "Sign up", new_user_registration_path %> -->
  <div class="d-flex justify-content-center">
    <% if user_signed_in? %>
    <!--<%= current_user.email %>-->
      <%= link_to 'Sign out', destroy_user_session_path, method: 'delete', :class => "btn btn-success m-2"  %>
    <% else %>
      <%= link_to 'Sign up', new_user_registration_path, :class => "btn btn-success m-2" %>
      <%= link_to 'Login', new_user_session_path, :class => "btn btn-success m-2"%>
    <% end %>

    <%= link_to 'Rent out your dog!', new_dog_path, :class => "btn btn-success m-2" %>
  </div>
